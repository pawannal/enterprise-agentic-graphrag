# MCP Architecture

The Model Context Protocol, also called MCP, provides a standardized way for AI agents to interact with external tools and data sources.

Project Orion uses MCP to connect agents with enterprise tools.

Project Phoenix uses MCP for approved tool integration.

MCP tools must be registered, authenticated, authorized, and monitored.

The Security Engineering Team defines permission requirements for MCP tools.

The AI Platform Team manages the integration architecture.