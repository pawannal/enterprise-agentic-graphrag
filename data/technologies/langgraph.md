# LangGraph

LangGraph is an orchestration framework used to build stateful AI agent workflows.

Project Phoenix uses LangGraph for agent orchestration.

Project Orion also uses LangGraph to coordinate multiple agent steps.

LangGraph allows AI applications to model workflows involving agents, tools, state, and decision-making.

LangGraph is sometimes described internally as the agent orchestration layer.