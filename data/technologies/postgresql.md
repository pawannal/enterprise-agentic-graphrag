# PostgreSQL

PostgreSQL is a relational database used by NexaAI for structured application data.

Project Atlas uses PostgreSQL for storing processed enterprise data.

Project Sentinel uses PostgreSQL for structured security and audit information.

PostgreSQL is complementary to Neo4j because relational data and graph relationships serve different purposes.